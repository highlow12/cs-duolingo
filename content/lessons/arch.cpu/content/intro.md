# CPU가 명령을 실행하는 순서

CPU는 메모리의 명령을 가져오고(fetch), 어떤 동작인지 해석한 뒤(decode), ALU와 레지스터 등을 사용해 실행(execute)합니다. 이 사이클을 매우 빠르게 반복하며 프로그램을 진행합니다.

명령어 주소를 가리키는 program counter와 계산 중인 값을 담는 register처럼 역할이 다른 저장 공간이 있습니다. CPU 내부의 작은 저장 공간을 구분하면 코드 한 줄이 실행되는 경로를 상상하기 쉬워집니다.
