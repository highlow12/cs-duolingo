# 함수로 동작을 묶기

함수는 함께 실행할 문장에 이름을 붙인 재사용 단위입니다. def로 정의하고, 괄호 안에 매개변수를 적어 입력을 받습니다.

~~~python
def greet(name):
    return "안녕, " + name

message = greet("하나")
~~~

함수를 정의하는 순간에는 본문을 저장하고, greet("하나")처럼 호출할 때 본문이 실행됩니다. 같은 로직을 여러 곳에서 호출해도 규칙은 한 곳에서 관리합니다.
