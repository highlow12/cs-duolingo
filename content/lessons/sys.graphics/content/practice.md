## 렌더링의 흐름

3D 그래픽에서는 정점(vertex)으로 도형을 정의하고, 화면 좌표로 변환한 뒤, 래스터화 단계에서 픽셀 조각을 만듭니다. 최종 색은 프레임버퍼에 기록되어 디스플레이에 전달됩니다.

입문에서는 정점 → 픽셀 → 프레임버퍼의 흐름과 색상 채널의 역할을 연결해 보세요. 실제 GPU 파이프라인은 더 많은 단계와 병렬 처리를 포함하지만 이 모델로 기본 용어를 구분할 수 있습니다.
