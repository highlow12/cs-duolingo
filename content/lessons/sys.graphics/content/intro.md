# 화면에 그림을 만드는 데이터

래스터 이미지는 화면을 작은 픽셀 격자로 나누고 각 픽셀의 색을 저장합니다. RGB 색상은 보통 빨강·초록·파랑 채널의 값으로 표현하며, 채널 값을 조합해 다양한 색을 만듭니다.

벡터 그래픽은 선과 도형의 기하 정보로 장면을 표현합니다. 확대할 때 픽셀을 그대로 늘리는 래스터와 달리, 도형을 다시 계산할 수 있다는 차이가 있습니다.
