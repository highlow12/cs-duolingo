# 반복문으로 같은 작업 반복하기

반복문은 같은 동작을 여러 번 적는 대신, 반복할 규칙을 한 곳에 적게 해 줍니다. for는 리스트처럼 순서가 있는 값에서 원소를 하나씩 꺼낼 때 잘 맞고, while은 조건이 참인 동안 계속 실행할 때 사용합니다.

~~~python
for name in ["민", "준"]:
    print(name)
~~~

들여쓴 블록이 한 번의 반복에서 실행됩니다. name은 첫 번째 반복에서 "민", 두 번째 반복에서 "준"을 가리킵니다.
