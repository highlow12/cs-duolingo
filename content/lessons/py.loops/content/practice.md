## 횟수와 누적값

range(3)은 0, 1, 2를 차례로 내놓아 세 번 반복합니다. 반복 횟수와 시작 값의 관계를 확인할 때 유용합니다.

합계처럼 반복마다 값을 모으는 변수는 반복문 바깥에서 먼저 시작합니다.

~~~python
total = 0
for number in [2, 4, 6]:
    total += number
~~~

반복이 끝나면 total은 12입니다. 반복문을 읽을 때는 각 회차의 변수 값과 누적값을 표로 적어 보면 실수를 줄일 수 있습니다.
