## 추가, 삭제, 길이

append는 마지막에 원소를 추가하고 pop은 기본적으로 마지막 원소를 꺼내면서 삭제합니다. len은 현재 원소 개수를 알려 줍니다.

~~~python
tasks = ["읽기"]
tasks.append("쓰기")
last = tasks.pop()
~~~

리스트를 다른 변수에 대입하면 같은 리스트를 가리킬 수 있습니다. 원본을 함께 바꾸고 싶지 않다면 복사 방법을 별도로 선택해야 합니다.
