# 리스트에 여러 값 담기

리스트는 여러 값을 순서대로 묶는 자료형입니다. 첫 원소의 인덱스는 0이며 scores[1]처럼 대괄호로 특정 위치를 읽습니다.

~~~python
scores = [80, 95, 70]
best = scores[1]
~~~

리스트는 변경 가능합니다. scores[0] = 85처럼 기존 위치의 값을 바꿀 수 있고, 같은 리스트를 반복문과 함께 순회할 수 있습니다.
