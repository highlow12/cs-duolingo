# 논리 연산과 진리표

명제 `P`, `Q`를 결합하면 더 복잡한 명제를 만들 수 있습니다.

- NOT `¬P`: `P`의 진리값을 뒤집습니다.
- AND `P ∧ Q`: `P`와 `Q`가 **모두** 참일 때만 참입니다.
- OR `P ∨ Q`: `P`와 `Q` 중 **하나 이상**이 참이면 참입니다.

진리표는 가능한 입력 조합을 빠짐없이 적고 결과를 계산하는 표입니다. 명제 두 개의 입력은 `참·참`, `참·거짓`, `거짓·참`, `거짓·거짓` 네 가지 조합을 가집니다.
