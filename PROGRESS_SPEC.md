# 로컬 학습 및 복습 정책 v1

## 문제 방문과 제출

QuestionHost는 QUESTION_SPEC.md대로 유효한 제출마다 `onAttempt`를 호출한다. 첫 오답 뒤 한 번의 재시도가 필요하며 첫 오답에서는 정답을 공개하지 않는다.

앱 연결 계층은 한 문제 방문의 최대 두 제출을 최종 `onCompleted` 요약으로 모아 저장한다. 저장 API의 `correct`는 **첫 시도 정답 여부**다. 따라서 첫 오답 후 재시도 정답도 false다. 원본 답안과 개별 키 입력은 장기 저장하지 않는다. 현재 MVP는 중단된 미완료 답안이 아니라, 최종 처리된 문제 방문을 StudyEvent로 보존한다.

| 방문 결과 | 저장 결과 | FSRS 등급 |
| --- | --- | --- |
| 첫 시도 정답 | correct | Good |
| 첫 오답, 두 번째 정답 | incorrect | Again |
| 첫 오답, 두 번째 오답 | incorrect | Again |

최종 제출 UUID를 idempotency key로 사용한다. 저장 오류 후 재시도에는 같은 ID와 같은 시간을 전달한다. 새로운 방문에서 처음 맞힌 답은 과거 오답과 관계없이 Good이다. `durationMs`는 이번 방문에서 실제로 답을 고른 활성 시간의 합이며 백그라운드·피드백·저장 대기는 제외한다.

## 저장과 세션

UI는 `learningRepository`를 통해서만 저장한다. StudyEvent, 문제/레슨 상태, 게임 이벤트와 상태, Outbox, 기기 순번을 Dexie 트랜잭션에서 함께 갱신한다. 실패하면 전체를 되돌리고 다음 단계 이동을 막는다.

레슨 세션의 현재 단계와 완료한 문제 요약을 저장한다. 이미 저장된 문제에서 새로고침하면 재제출하지 않고 다음 단계로 계속할 수 있다. 레슨 revision이 달라지면 세션을 새로 시작한다. 완료된 레슨을 다시 읽어도 완료 상태는 유지한다.

## 복습

`ts-fsrs` Adapter를 사용한다. 목표 retention은 0.90이며 기본 파라미터를 프로필에 보존한다. 재현 가능한 백업 replay를 위해 fuzz를 끈다. 앱 코드에는 자체 FSRS 근사식을 두지 않는다.

학습한 문제 중 예정 시간이 지난 것만 복습 큐에 넣는다. 새로운 문제는 커리큘럼을 통해 처음 학습한다. 재학습 문제를 먼저, 이어 일반 복습과 학습 중인 문제를 정렬한다. 앱에서 이번 세션의 최대 문제 수를 제한한다. 이전에 학습한 문제의 revision이 바뀌면 새 스케줄러 상태로 다시 학습하도록 예약한다.

## 경험치와 날짜

경험치는 별도 정책 모듈에서 정한다. 첫 시도 정답에 경험치를 부여한다. 학습한 날짜는 사용자의 로컬 달력 날짜로 기록한다. 현재 연속 학습은 오늘 또는 어제의 기록이 이어진 경우만 표시하고 최장 기록은 보존한다.

## 백업

JSON 백업은 StudyEvent, SchedulerProfile, GameEvent, Settings와 세션 보조 정보를 담는다. 문제 상태와 레슨 상태는 이벤트를 replay해 만든다. 복원은 현재 기록을 교체하며 사용자가 확인한 뒤 수행한다. 형식·버전·ID·수치·참조가 잘못된 백업을 거부하고 실패 시 기존 DB를 보존한다. 자동 동기화와 여러 기기 병합은 아직 제공하지 않는다.
